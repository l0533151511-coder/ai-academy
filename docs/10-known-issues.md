# בעיות ידועות ותובנות טכניות

## ביצועי קומפילציה בפיתוח (Dev Server)
נצפו זמני קומפילציה איטיים מאוד (עד 80+ שניות) בקומפילציית קרה (cold start) של Next.js/Turbopack על הפרויקט ב-`F:\AI_Academy\app`. הסיבה הסבירה: אזהרת Next.js "Slow filesystem detected" — כונן F: איטי יחסית לקריאה/כתיבה של קבצים רבים. לאחר קומפילציה ראשונה, HMR (עדכונים חמים) מהיר בהרבה.
**המלצה עתידית (לא בוצעה, ממתינה להחלטת המשתמש):** להעביר את `.next` (תיקיית ה-cache) או את הפרויקט כולו לכונן מהיר יותר (C:) אם זה הופך לחסם עבודה משמעותי.

## עקרון מודולריות ורכיבים לשימוש חוזר
מרגע זה (בקשת המשתמש), כל רכיב אינטראקטיבי חדש (סימולטור, ויזואלייזר, קנבס, פלייגראונד) נבנה מתוך כוונה לשימוש חוזר:
- **PixelDrawPad** — קנבס ציור פיקסלים גנרי (לא ספציפי לזיהוי ספרות) — יכול לשמש בעתיד לכל שיעור שדורש ציור/סימון ידני.
- **NeuralNetworkVisualizer** — מקבל מבנה שכבות כפרמטר (`layers: number[]`) — לא קשור לתוכן שיעור ספציפי, ישמש גם בשיעורי טרנספורמר/attention בהמשך.
- **EngineeringInsights** — רכיב תוכן סמנטי (לא רק עיצובי) לשמירה על עקביות "לחשוב כמו מהנדס" בכל שיעור AI מרכזי.
- **RoleSort** — כבר שימש ב-2 הקשרים שונים (client/server, סוגי למידה) — דוגמה טובה לעיקרון "רכיב אחד, כמה שיעורים".

כלל אצבע להמשך: לפני בניית רכיב חדש, לבדוק קודם אם קיים רכיב דומה שניתן להרחיב עם props נוספים, במקום לשכפל קוד.

## תקלה אמיתית שנתפסה ותוקנה: Service Worker הגיש קוד ישן ב-dev
לאחר הוספת ה-Service Worker (לתמיכת PWA), תוקן באג בחיפוש הגלובלי (נירמול אותיות סופיות), אך
הבדיקה החיה בדפדפן הראתה שהתיקון "לא עובד" — למרות שה-build/typecheck עברו נקי והלוגיקה נבדקה
נכונה במבודד. הסיבה: ה-SW (שנרשם גם בסביבת dev) הגיש **גרסה ישנה של קובץ ה-JS מהקאש** במקום
הגרסה החדשה מ-Turbopack/HMR — קלאסי "באג רפאים" שבו שינוי אמיתי בקוד לא משתקף בדפדפן.
**התיקון:** ה-Service Worker נרשם כעת **רק ב-production** (`process.env.NODE_ENV === "production"`)
— לעולם לא ב-dev. **לקח לזכור:** אם שינוי קוד "לא משפיע" על ההתנהגות למרות build/typecheck נקיים,
לבדוק את ה-Service Worker (unregister + נקה caches) לפני שמחפשים באג במקום אחר.

## תקלה אמיתית שנתפסה ותוקנה: כותרות דף כפולות
לאחר הוספת `title.template: "%s | אקדמיית AI"` ל-layout השורש (כחלק מסבב שיפור SEO), כל ~35 קובצי
`layout.tsx` של שיעורים כבר הכילו את הסיומת הזו בעצמם (`title: "X | אקדמיית AI"`), מה שגרם לכותרות
כפולות בפועל ("X | אקדמיית AI | אקדמיית AI") — התגלה בבדיקה ידנית עם preview_snapshot, לא ב-build.
**התיקון:** הוסרה הסיומת הידנית מכל קובצי ה-metadata (script אוטומטי + תיקון ידני לקובץ אחד עם
גרש פנימי שקטע את ה-regex). **לקח לזכור:** שינוי ב-layout השורש (כמו title template) יכול להשפיע
בשקט על עשרות קבצים — תמיד לבדוק render בפועל של עמוד אחד לפחות אחרי שינוי כזה, לא רק build ירוק.
