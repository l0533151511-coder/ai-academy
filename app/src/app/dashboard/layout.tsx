export const metadata = {
  title: "לוח הבקרה שלי",
  description: "התקדמות, רצף למידה, המסלול המותאם אישית והמשך מהמקום שבו הפסקת.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
